# apps/

Reference and test applications built on Hius — a two-domain vertical
slice proving the framework end to end, and later dogfooding scaffolding.
Not published packages; matched by the `apps/*` workspace glob in the
root `package.json`.
