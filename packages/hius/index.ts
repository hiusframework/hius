import { PACKAGE_NAME as SPEC_PACKAGE_NAME } from "@hius/spec";

// Placeholder — discovery, explicit composition, events, ts-morph
// extraction, and the Query AST/Encryption Layer land here.
export const PACKAGE_NAME = "hius" as const;
export { SPEC_PACKAGE_NAME };
