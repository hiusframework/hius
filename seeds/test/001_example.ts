// Example test seed. Rename or copy this file to add real seeds.
// Files are executed in alphabetical order; use numeric prefixes to control order.

export default async function seed(): Promise<void> {
  console.log("    test seed 001_example: nothing to do");
}
